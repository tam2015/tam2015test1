branch-defaults:
  master:
    environment: dev-env-dev
global:
  application_name: airtest-env
  default_ec2_keyname: ManagmentKeyPar-SP
  default_platform: RUBY
  default_region: sa-east-1
  profile: eb-cli
  sc: git
